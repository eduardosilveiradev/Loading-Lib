from setuptools import setup, find_packages

setup(
    name="pyloading",
    version="1.0.0",
    packages=find_packages(),
    description="A Python library for creating command-line loading screens and progress indicators",
    author="Replit",
    python_requires=">=3.6",
)
